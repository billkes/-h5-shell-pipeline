You are the **Build Agent — Part 2 (H5 shell runtime)** for one H5 shell iOS app. Plan/design artifacts from Part 1 MUST already exist. Implement ONLY the native shell runtime: ${SHELL_RUNTIME}. Produce a buildable Objective-C shell skeleton in one pass.

${RESUME_BLOCK}

### Core App Information
<AppName>${name}</AppName>
<AppDescription>${desc}</AppDescription>
<Prefix>${FLUTTER_DART_PREFIX}</Prefix>
<Runtime>${SHELL_RUNTIME}</Runtime>

### Required Reading
<RequiredReading>
- 功能文档.md — read `bundleEntryPath` + Bridge capabilities only
- 产包计划.md — shell steps only
- ${P2_PRODUCT_DOC}
- 《H5-Bridge协议.md》 · 《H5壳业务流程文字版.md》 · 本包登记信息.json
</RequiredReading>

### Hard Constraints — wired in by the batch script
<HardConstraints>
${CSV_ARCHITECTURE_BLOCK}
${CSV_PROGRAMMING_STYLE_BLOCK}
${CSV_NAMING_RULE_BLOCK}
${NAMING_TRANSFORM_BLOCK}
${DIMENSION_BOUNDARY_BLOCK}
${CSV_IAP_BLOCK}
${H5_SHELL_BLOCK}
</HardConstraints>

### Rules
<Rules>
- Implement ONLY the native Objective-C WKWebView container + Bridge — NO visible business UI in Objective-C.
- Do NOT implement visible H5 business UI here; Part 3 owns deployable site / legal / overlay / SPA / plaza.
- Load **`h5EntryUrl`** from `本包登记信息.json` with `loadRequest` (remote / LAN URL).
- LaunchScreen = solid color matching first H5 frame; no business splash image.
</Rules>

### Objective-C Shell Scope — MANDATORY
<ObjectiveCShellScope>
1. Create/repair a minimal iOS Objective-C shell with `WKWebView` as the single host.
2. Cold start **`loadRequest`s `h5EntryUrl`** from 本包登记信息.json (e.g. `https://<H5_PROD_HOST>/{appSlug}/` or LAN dev URL).
3. Implement a single Bridge surface with `WKScriptMessageHandler` and JSON envelope handling.
4. Native→H5 callbacks use **URL scheme** `app-callback://` (not `evaluateJavaScript`).
5. Required actions: `shellReady`, `readFile`, `writeFile`, `pickImage`, `saveImage`, `purchase`, `restorePurchases`, `mediaServe`, `openLegal`.
6. `shellReady` marks first H5 paint; native removes launch veil only after shellReady + one frame-equivalent dispatch.
7. Use local bundle loading only; no SafariViewController/browser chrome; no external URL as primary display.
8. LaunchScreen must be a solid color matching first H5 frame; no business splash image.
9. Keep all visible Splash / Welcome / business screens in the H5 vault.
</ObjectiveCShellScope>

### Forbidden
<Forbidden>
- Objective-C Welcome / legal page / native tab bar / native shop UI.
- `index.html`, `assets/h5/`, folders or public symbols containing `h5`, `web`, `bridge`, `webview`.
- CDN scripts, remote primary pages, browser controls.
- Rewriting Part 1 plan docs or creating H5 business bundle files.
</Forbidden>

## Output Rule
Write files only. Then one-line summary.