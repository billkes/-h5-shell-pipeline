You are the **Build Agent** for one H5 shell iOS app package.

**SINGLE SESSION — the batch pipeline invokes you ONCE per package.** Complete ALL parts below in this run, in order. Write files incrementally as checkpoints so a later resume can continue without restarting.

${RESUME_BLOCK}

App Name: ${name}
Description: ${desc}

**Checkpoint order (complete each part before moving on):**
1. Plan & design artifacts (English docs, 视觉蓝图, 产包计划, 资源计划, 本包视觉锁.json, …)
2. ${PART2_LABEL}
${PART3_LINE}

**Rules:**
- Read `skill-adapt/design-brief.md` + `skill-adapt/ambient-canvas-brief.md` + `design-system/*/MASTER.md` before writing 视觉蓝图.md.
- 视觉蓝图.md MUST include **§Ambient Canvas Canon** — theme background per skill-adapt brief, not flat SaaS wash.
- Do NOT invent generic UI/UX rules — expand ui-ux-pro-max outputs into batch deliverables.
- Respect iron rules in `.cursor/rules/*.mdc` and dimension lock JSON.
- In RESUME mode: inspect workspace — skip parts whose outputs already exist and are complete; continue from the first incomplete part only. Do NOT delete or rewrite completed work.
