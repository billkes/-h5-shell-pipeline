# Python 字节码缓存
__pycache__/
*.py[cod]
*$py.class

# 虚拟环境
.venv/

# 本地 API Key / 配置，勿提交
config/config.env
config/config.yaml

# 计划生成临时文件
.plan-gen/
.batch-queue-*.txt

# 批次产出目录（体积大，勿提交）
output/

# pick-theme 候选池 sidecar（按主题一次性，不跨批次复用）
task.csv.name-pools.json
