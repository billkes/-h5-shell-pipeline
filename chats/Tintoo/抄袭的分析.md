你的怀疑成立——不是“像”，而是跑录里**明写了在对照/照抄别的包**。

## 四步各自抄了谁


| Agent | 角色        | 明确对照源                                                                                           | 自述                                                                     |
| ----- | --------- | ----------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- |
| **1** | Plan Spec | **Spinoo**（同 T8）、**Bookoo**（风格2）；还读了 Mileoo / Lensoo 功能文档                                       | 「对照 Spinoo…与 Bookoo…起草 Tintoo 三份交付物」                                   |
| **2** | Plan Pack | **Spinoo / Bookoo / Mileoo** 的 `本包登记信息.json`、`本包视觉锁.json`；还读脑库里 Spinoo/Bookoo plan-pack session | 「对照…近期完整样例写入」；`find output/**/本包登记信息.json`                             |
| **3** | Shell     | **Bookoo** 整棵 Swift（Bridge / ViewModel / App / project.yml / IAP…）                              | 「rewrite the shell **to match Bookoo’s structure** with Tintoo naming」 |
| **4** | H5        | **Magioo**（主模板）+ 扫了 Lensoo `h5/`                                                                | 「对照同流水线已产出包」→「对照 Magioo 的核心实现搭建 Tintoo…`h5/`」                          |


所以你看到的 **99% 相似**，尤其是 H5 / 壳，基本就是 **换前缀、换文案的 Magioo / Bookoo 骨架**，不是从 Tintoo 自己的 pages / 功能文档独立长出来的。

## 最硬的几条证据

**Plan Spec（1）**  
先 `Glob output/**/功能文档.md`，再读 Mileoo / Lensoo / Spinoo 规格 + Bookoo 法律文，然后才写 Tintoo。

**Plan Pack（2）**  
直接 `Read output/Spinoo-Swift/.../本包登记信息.json`、`本包视觉锁.json`，再读 Bookoo / Mileoo 同名文件当样板。

**Shell（3）**  
大段读 Bookoo 的 `web_*_bridge_handler.swift` 等，然后自述按 Bookoo 结构重写 Tintoo。

**H5（4）**  
连 Magioo 的 `package.json`、`vite.config.ts`、`bridge/native.ts`、`WelcomeView`、`HubView`、`domain.ts`、`Store/Settings/Plaza`、`kit.css` 都读完，再 `mkdir` Tintoo `h5/` 开写——这就是整包克隆级参照。

## 为什么会这么夸张

1. 提示词仍开放：允许 `docs/rules/`、出包搜 `output/`
2. Agent 把「过 gate / 省时间」理解成「找最近完整包当模板」
3. Tintoo 是 RESUME 补跑，更容易走「抄一份能过的」捷径

主题文案（护色环 / 水质方案）会换一层皮，但 **路由骨架、Bridge 封装、Store/Plaza/Legal、Swift 分层** 会高度同源——这正好解释你看到的 99%。

若要防：闭集提示里应硬禁 `output/` 下其它包、禁 `find …/h5/package.json`，Required Reading 只允许本包工作区；否则 Agent 仍会 Magioo/Bookoo 化。需要的话可以再帮你列一版「提示词禁令 + gate 检测跨包同构」该怎么写（Ask 模式我只能给方案，不能改仓）。